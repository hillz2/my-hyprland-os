general {
    lock_cmd = pidof hyprlock || hyprlock       # what to run when dbus asks to lock
    before_sleep_cmd = loginctl lock-session    # lock before suspend
    after_sleep_cmd = hyprctl dispatch dpms on  # turn display on when waking
}
